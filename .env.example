# VeChain DApp Environment Configuration
# ---------------------------------
# IMPORTANT: NEVER commit the actual secrets to Git - this file is just a template.
# Copy this file to .env and add your real values there.

# VeChain Network Configuration
# Options: 'test' (TestNet), 'main' (MainNet), 'solo' (Local development network)
VITE_REACT_APP_VECHAIN_NETWORK=test

# Smart Contract Configuration
# The address of your deployed NFT contract on the selected network
VITE_REACT_APP_NFT_CONTRACT_ADDRESS=your_contract_address_here

# Private Key for Wallet Authentication (HIGHLY SENSITIVE)
# Format: 0x + 64 hexadecimal characters (without 0x prefix)
# Generate a development key with: node temp-key-gen/generateKey.js
# WARNING: NEVER use production/real keys in development or commit to Git
VITE_VECHAIN_PRIVATE_KEY=your_private_key_here

# API Configuration
# The base URL for API requests (used in development)
VITE_API_URL=http://localhost:5000

# WebSocket Configuration (optional)
# For local development, should match the server WebSocket endpoint
VITE_WS_URL=ws://localhost:5000/ws